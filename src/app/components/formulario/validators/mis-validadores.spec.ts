import { MisValidadores } from './mis-validadores';

describe('MisValidadores', () => {
  it('should create an instance', () => {
    const directive = new MisValidadores();
    expect(directive).toBeTruthy();
  });
});
